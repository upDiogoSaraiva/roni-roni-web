# dependências (não há runtime deps, mas por segurança)
node_modules/

# estado mutável gerado em runtime (recriado a partir do seed no 1.º arranque)
data/registry.json
data/store.json
data/competitions/*/store.json
# exceção: edições TERMINADAS ficam no repo como registo histórico (o store é o arquivo final)
!data/competitions/wc2026/store.json

# identidades de dispositivo (tokens) e backups automáticos — dados, não código
data/identities.json
*.bak

# backups automáticos do estado (cópia .bak antes de cada gravação)
*.bak

# screenshots de teste e artefactos do browser
/*.png
.playwright-mcp/
.claude/

.DS_Store
Thumbs.db
DESIGN_motor_ref.md

# (até 20/07/2026 a edição real vivia fora do repo; com o torneio encerrado, o grupo
#  decidiu guardá-la no git como registo histórico — ver README)
